export interface IContactForm {
  name: string;
  email: string;
  message: string;
  subject: string;
}

export const CONTACT_FORM_ENUMS = {
  name: "name",
  email: "email",
  message: "message",
  subject: "subject",
};
